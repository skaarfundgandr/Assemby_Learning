## Add two assembly
Simple program that adds two numbers (23 & 19) in assembly and then prints out the result to stdout.

Run using
```bash
just
```
or manually assemble the file using
```bash
just assemble add_two.asm
```
and then run after assembling using
```bash
just run
```
