## Add two numbers input
Simple assembly program to add two numbers from STDIN and print the sum.

Run using
```bash
just
```
or manually assemble the file using
```bash
just assemble add_two_input.asm
```
and then run after assembling using
```bash
just run
```
